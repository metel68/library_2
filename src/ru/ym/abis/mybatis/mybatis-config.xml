<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>

	<properties resource='mysql.jdbc.properties' />

	<settings>
		<setting name="aggressiveLazyLoading" value="false" />
		<setting name="logImpl" value="STDOUT_LOGGING"/>
	</settings>
	
	<typeAliases>
		<typeAlias alias="User" type="ru.ym.abis.models.User" />
		<typeAlias alias="Book" type="ru.ym.abis.models.Book" />
		<typeAlias alias="Author" type="ru.ym.abis.models.Author" />
		<typeAlias alias="Publisher" type="ru.ym.abis.models.Publisher" />
	</typeAliases>

	<environments default="development">
		<environment id="development">
			<transactionManager type='JDBC' />
			<dataSource type='POOLED'>
				<property name='driver' value='${jdbc.driverClassName}' />
				<property name='url' value='${jdbc.url}' />
				<property name='username' value='${jdbc.username}' />
				<property name='password' value='${jdbc.password}' />
			</dataSource>
		</environment>
	</environments>

	<mappers>
		<mapper resource="mappers/author.xml"/>
		<mapper resource="mappers/book.xml"/>
    	<mapper resource="mappers/publisher.xml"/>
		<mapper resource="mappers/user.xml"/>
	</mappers>

</configuration>